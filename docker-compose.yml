services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8002:8000"
    volumes:
      - .:/app
      - /app/__pycache__/
    command: uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
