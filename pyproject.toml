[project]
name = "mindsherpa-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "debugpy>=1.8.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
